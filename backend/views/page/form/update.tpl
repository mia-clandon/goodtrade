{$form_start}

<div class="card">
    <div class="card-header">Основная информация</div>
    <div class="card-body">
        {$title}
        {$alias}
        {$text}
    </div>
</div>

{$submit}
{$form_end}